{% extends 'base.html.twig' %}

{% block title %}Ajouter une série{% endblock %}

{% block body %}
    <h1 class="d-flex justify-content-center m-5 " >Ajouter une série</h1>

    {{ include('program/_form.html.twig') }}

    <div class="container-fluid">
        <a class="btn btn-bleuc" href="{{ path('program_index') }}"> <- Retour à la liste</a>
    </div>
{% endblock %}
