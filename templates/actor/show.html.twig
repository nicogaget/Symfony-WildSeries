{% extends 'base.html.twig' %}

{% block title %}Actor{% endblock %}

{% block body %}
<h1 class="d-flex justify-content-center m-5"> Fiche Acteur</h1>

<div class="container-fluid table-show" >
    <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ actor.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="program-footer d-flex justify-content-around">

        <a class="btn btn-bleuc" href="{{ path('actor_index') }}"> <- Retour à la liste</a>

        <a  class="btn btn-bleuf" href="{{ path('actor_edit', {'slug': actor.slug}) }}">Editer</a>

        {{ include('actor/_delete_form.html.twig') }}
    </div>
</div>

{% endblock %}
